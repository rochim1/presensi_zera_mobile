enum ApprovalType { single, sequential, parallel }

enum ApprovalHistoryStatus { in_progress, approved, rejected }

enum ApprovalStatus { pending, approved, rejected }

enum ApprovalRequestType {
  lembur,
  request_presensi,
  shift_swap,
  reimbursement,
  cuti,
  inventory_transfer,
  inventory_purchase,
  inventory_opname,
  payroll_batch,
}
